<?xml version="1.0" encoding="utf-8"?>
<!-- Cleartext is permitted only for the self-hosted webhook host(s): the loopback
     used by `adb reverse` over USB, and the LAN IP of the dev machine. All other
     traffic stays cleartext-blocked (the app's default at targetSdk 34). -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">192.168.1.47</domain>
    </domain-config>
</network-security-config>
